#include<stdio.h>

/*
  Isertion Sort algorithm 

  :INPUT:
    A: ptr to and array of int
    array_sz: Int, size of the input array A
  :OUT:
    sorted A
*/
void insertion_sort(int* A, int array_sz, int verbose);